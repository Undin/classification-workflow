apply plugin: 'kotlin'

dependencies {
    compile project(':core')
    compile project(':baseline')
    compile project(':master')
    compile project(':experiments')

    compile libraries.kotlin

    compile libraries.jongo
    compile libraries.mongo

    compile libraries.poiXml
}
